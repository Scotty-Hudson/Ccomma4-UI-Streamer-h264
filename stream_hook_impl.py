"""DEPRECATED — part of the abandoned .pth hook approach.

See stream_hook.py for why this was removed.
The capture logic now lives in ui_stream.py's capture_frame() function.
ensure_stream.sh removes this file from /data/ during installation.
"""
